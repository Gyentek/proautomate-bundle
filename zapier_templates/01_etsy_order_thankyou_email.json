{
  "name": "Etsy Order → Thank-You Email (Zapier)",
  "description": "Zapier version: automatic thank-you email on a new Etsy order",
  "trigger": {
    "app": "Etsy",
    "event": "New Order",
    "account": "{{ETSY_ACCOUNT}}",
    "config": {
      "include_digital": true,
      "include_physical": true
    }
  },
  "actions": [
    {
      "step": 1,
      "app": "OpenAI (GPT-4, DALL-E, Whisper)",
      "event": "Send Chat Message",
      "account": "{{OPENAI_ACCOUNT}}",
      "config": {
        "model": "gpt-4o-mini",
        "user_message": "Write a warm and professional thank-you email for the following Etsy order (in HTML format, in English):\n\nCustomer: {{1__buyer_name}}\nOrder No: {{1__order_id}}\nProduct: {{1__title}}\nAmount: {{1__grand_total}} {{1__currency_code}}\nOrder Date: {{1__created_timestamp}}\n\nSummarize the order details in the email, build excitement about the product, and politely ask for a 5-star review.",
        "system_message": "You are a customer service specialist for an e-commerce store. You write personalized emails that make customers feel special.",
        "max_tokens": 600
      }
    },
    {
      "step": 2,
      "app": "Gmail",
      "event": "Send Email",
      "account": "{{GMAIL_ACCOUNT}}",
      "config": {
        "to": "{{1__buyer_email}}",
        "from": "{{SENDER_EMAIL}}",
        "name": "{{SENDER_NAME}}",
        "subject": "🎉 Thank You! Your order #{{1__order_id}} has been received",
        "body_type": "html",
        "body": "{{2__choices__0__message__content}}",
        "reply_to": "{{REPLY_TO_EMAIL}}"
      }
    },
    {
      "step": 3,
      "app": "Google Sheets",
      "event": "Create Spreadsheet Row",
      "account": "{{GOOGLE_ACCOUNT}}",
      "config": {
        "spreadsheet": "{{SPREADSHEET_ID}}",
        "worksheet": "Orders",
        "Date": "{{zap_meta_humanized_time}}",
        "Order_No": "{{1__order_id}}",
        "Customer": "{{1__buyer_name}}",
        "Email": "{{1__buyer_email}}",
        "Product": "{{1__title}}",
        "Amount": "{{1__grand_total}}",
        "Email_Sent": "Yes"
      }
    }
  ],
  "_SETUP_NOTES": {
    "steps": [
      "1. Log in to Zapier.com",
      "2. Click the 'Create Zap' button",
      "3. Trigger: Etsy → New Order",
      "4. Action 1: OpenAI → Send Chat Message",
      "5. Action 2: Gmail → Send Email",
      "6. Action 3: Google Sheets → Create Row",
      "7. Connect your account and map the fields at each step",
      "8. Test it and turn the Zap on"
    ],
    "tip": "You can't import this JSON directly into Zapier, but this file shows you exactly how to configure each step."
  }
}
